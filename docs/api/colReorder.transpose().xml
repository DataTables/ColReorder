<?xml version="1.0" encoding="UTF-8" ?>
<dt-api library="ColReorder">
	<name>colReorder.transpose()</name>
	<summary>Convert one or more original column indexes to their current index</summary>
	<since>1.3.0</since>

	<type type="function">
		<signature>colReorder.transpose( idx )</signature>
		<parameter name="idx" type="integer|array">

		</parameter>
		<description>
			Get one or more current column indexes form their original index.
		</description>
		<returns type="integer|array">The transpose values</returns>
	</type>

	<description>
		ColReorder will change the indexes of columns when columns are reordered. This can mean that if you have a reference to a column already (`dt-api column().index()`) it might need to be updated because of the change in the column ordering. For example consider column index 0 is moved by the end user to index 5. You need to find out what its index is - this method provides that ability.
	</description>

	<example title="Get the current column position of the column that was in index 0 when the table was created"><![CDATA[

var table = $('#example').DataTable( {
	colReorder: true
} );

$('#info').click( function (e) {
	alert( table.colReorder.transpose( 0 ) );
} );

]]></example>

</dt-api>