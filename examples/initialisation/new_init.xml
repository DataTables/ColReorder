<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="6">

<css lib="datatables colreorder" />
<js lib="jquery datatables colreorder">
<![CDATA[
var table = $('#example').DataTable();

new DataTable.ColReorder(table);
]]>
</js>

<js-vanilla>
<![CDATA[
let table = new DataTable('#example');

new DataTable.ColReorder(table);
]]>
</js-vanilla>

<title lib="ColReorder">Initialisation using `new`</title>

<info><![CDATA[

As well as providing the option to be initialised through the `cr-init colReorder`, ColReorder can also be added to a DataTable using direct initialisation - `new DataTable.ColReorder();` as shown in this example. Options can be passed in as the second parameter for the constructor.

]]></info>

</dt-example>
